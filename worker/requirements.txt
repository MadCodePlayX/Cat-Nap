# ── Core worker deps ──────────────────────────────────────────────────────────
requests>=2.31.0
Pillow>=10.0.0
rembg==2.0.65          # background removal; pinned to match Hunyuan3D-2.1 reqs
onnxruntime-gpu>=1.17.0 # rembg backend — GPU-accelerated; falls back to CPU automatically
websocket-client>=1.7.0 # ComfyUI WS progress streaming (falls back to polling)

# ── PyTorch (CUDA wheels) ─────────────────────────────────────────────────────
# --extra-index-url lets pip find the CUDA builds here first, then fall back
# to PyPI for everything else in this file. This means you can simply run:
#   pip install -r requirements.txt
# and get CUDA-enabled torch/torchvision without any separate install step.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124

# ── Hunyuan3D-2.1 deps (pinned to match its requirements.txt) ─────────────────
# Repo: https://github.com/Tencent-Hunyuan/Hunyuan3D-2.1
# Not pip-installable (no setup.py). Clone it separately and pass
# --hunyuan-dir to the worker. These are the Python deps it needs at runtime.
transformers==4.46.0
diffusers==0.30.0
accelerate==1.1.1
huggingface-hub==0.30.2
safetensors==0.4.4
numpy==1.24.4
scipy==1.14.1
einops==0.8.0
trimesh==4.4.7
pymeshlab==2022.2.post3
pygltflib==1.16.3
xatlas==0.0.9
open3d==0.18.0
omegaconf==2.3.0
tqdm==4.66.5
opencv-python==4.10.0.84
imageio==2.36.0
scikit-image==0.24.0
